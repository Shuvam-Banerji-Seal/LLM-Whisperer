# Script Notes

Author: Shuvam Banerji Seal

These scripts are reference workflows and may require additional packages:

- `quantize_bnb.py`: `transformers`, `accelerate`, `bitsandbytes`
- `quantize_gptq.py`: `transformers`, GPTQ backend dependencies
- `quantize_awq.py`: `transformers`, AWQ backend dependencies
- `quantize_llama_cpp.sh`: llama.cpp binaries and conversion script

Always benchmark and validate output quality after quantization.
